import { styled } from "@/app/stitches.config";


const Container = styled("section", {
    height: "90vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
})


export {
    Container
}