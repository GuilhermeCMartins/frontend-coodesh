import { styled } from "@/app/stitches.config";


const LoginContainer = styled("section", {
    height: "90vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
})


export {
    LoginContainer
}